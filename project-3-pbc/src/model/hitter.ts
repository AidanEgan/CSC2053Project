export default interface Hitter {
    id: string, 
    atBats: number, 
    cs: number, 
    erp: number, 
    gidp: number, 
    h: number, 
    hbp: number, 
    playerID: string, 
    playerName: string, 
    sb: number, 
    tb: number, 
    walks: number,
};